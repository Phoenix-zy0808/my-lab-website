# 📋 最终修改方案总结 (2026 年 3 月)

> 修改状态：✅ 全部完成  
> 构建验证：✅ 通过  
> 修改时间：2026 年 3 月 29 日

---

## 📋 目录

- [一、已完成修改清单](#一已完成修改清单)
- [二、详细修改说明](#二详细修改说明)
- [三、验收标准](#三验收标准)
- [四、修改前后对比](#四修改前后对比)

---

## 一、已完成修改清单

| 优先级 | 修改项 | 文件 | 状态 |
|--------|--------|------|------|
| P0 | Footer 布局调整 | TheFooter.vue | ✅ 已完成 |
| P0 | 教育/工作经历年份字体增大 | team.vue | ✅ 已完成 |
| P0 | PDF 查看器修复 | PdfModal.vue | ✅ 已完成 |
| P0 | PDF 打开逻辑修复 | achievements.vue | ✅ 已完成 |
| P1 | Vite 构建配置 | vite.config.ts | ✅ 已完成 |

---

## 二、详细修改说明

### 【P0】修改 1：Footer 布局调整

**文件：** `src/components/TheFooter.vue`

**修改内容：**
- 联系方式：从 `lg:pl-16` (右移 4rem) 改为 `lg:-ml-16` (左移 4rem)
- 友情链接：从 `lg:pl-16` (右移 4rem) 改为 `lg:-ml-8` (左移 2rem)

**修改代码：**

```html
<!-- 联系方式 - 左移四个文字长度 -->
<div class="lg:col-span-1 lg:-ml-16">
  <h4 text-base font-semibold mb-4>联系方式</h4>
  ...
</div>

<!-- 友情链接 - 左移两个文字长度 -->
<div class="lg:col-span-1 lg:-ml-8">
  <h4 text-base font-semibold mb-4>友情链接</h4>
  ...
</div>
```

**修改对比：**

| 项目 | 修改前 | 修改后 |
|------|--------|--------|
| 联系方式 | `lg:pl-16` (右移 4rem) | `lg:-ml-16` (左移 4rem) |
| 友情链接 | `lg:pl-16` (右移 4rem) | `lg:-ml-8` (左移 2rem) |

---

### 【P0】修改 2：教育/工作经历年份字体增大

**文件：** `src/pages/team.vue`

**修改内容：**
- 教育经历年份字体：`text-sm` → `text-base`
- 工作经历年份字体：`text-sm` → `text-base`

**修改代码：**

```html
<!-- 教育经历 - 年份字体增大 -->
<div class="sm:w-56 flex-shrink-0">
  <span class="text-base text-gray-700 font-medium whitespace-nowrap">
    {{ edu.time }}：
  </span>
</div>

<!-- 工作经历 - 年份字体增大 -->
<div class="sm:w-56 flex-shrink-0">
  <span class="text-base text-gray-700 font-medium whitespace-nowrap">
    {{ work.time }}：
  </span>
</div>
```

---

### 【P0】修改 3：PDF 查看器修复

**文件：** `src/components/PdfModal.vue`

**修改内容：**
1. 添加 PDF 加载状态 (`pdfLoading`)
2. 添加 PDF 错误状态 (`pdfError`)
3. 添加加载中和错误提示 UI
4. 添加 `@load` 和 `@error` 事件处理
5. 修改"下载 PDF"按钮为"在新窗口打开"
6. 添加 `watch` 监听 modal 状态重置

**修改代码：**

```javascript
// PDF 加载状态
const pdfLoading = ref(true)
const pdfError = ref(false)

function close() {
  isOpen.value = false
  pdfLoading.value = true
  pdfError.value = false
}

// PDF 加载完成
function onPdfLoad() {
  pdfLoading.value = false
  pdfError.value = false
}

// PDF 加载失败
function onPdfError() {
  pdfLoading.value = false
  pdfError.value = true
}

// 监听 modal 打开，重置状态
watch(isOpen, (val) => {
  if (val) {
    pdfLoading.value = true
    pdfError.value = false
  }
})
```

**模板部分：**

```html
<!-- PDF 内容区域 -->
<div flex-1 overflow-hidden bg-gray-100 relative>
  <!-- 加载状态 -->
  <div v-if="pdfLoading && pdfUrl" flex items-center justify-center h-full>
    <div text-center text-gray-500>
      <div i-carbon-circle-dash animate-spin text-4xl mb-4 />
      <p>PDF 加载中...</p>
    </div>
  </div>

  <!-- 错误状态 -->
  <div v-else-if="pdfError || !pdfUrl" flex items-center justify-center h-full>
    <div text-center text-gray-500>
      <div i-carbon-document text-4xl mb-4 op="50" />
      <p v-if="!pdfUrl">未指定 PDF 文件</p>
      <p v-else>PDF 加载失败，请尝试在新窗口打开</p>
    </div>
  </div>

  <!-- PDF iframe -->
  <iframe
    v-if="pdfUrl && !pdfError"
    :src="pdfUrl + '#toolbar=0'"
    w-full
    h-full
    border-0
    title="PDF Preview"
    @load="onPdfLoad"
    @error="onPdfError"
  />
</div>
```

---

### 【P0】修改 4：PDF 打开逻辑修复

**文件：** `src/pages/achievements.vue`

**修改内容：**
- 移除 `encodeURI` 编码
- 确保路径以 `/` 开头
- 让浏览器自动处理路径

**修改代码：**

```javascript
// 打开 PDF
function openPdf(pdfUrl: string, title: string) {
  // 确保路径以/开头
  const normalizedUrl = pdfUrl.startsWith('/') ? pdfUrl : `/${pdfUrl}`
  // 不要使用 encodeURI，让浏览器自动处理
  currentPdfUrl.value = normalizedUrl
  currentPdfTitle.value = title
  showPdfModal.value = true
}
```

---

### 【P1】修改 5：Vite 构建配置

**文件：** `vite.config.ts`

**修改内容：**
- 添加 `build.assetsInclude` 配置
- 确保 PDF 资源正确处理

**修改代码：**

```typescript
export default defineConfig({
  // ... 原有配置 ...

  // 构建配置 - 确保 PDF 资源正确处理
  build: {
    assetsInclude: ['**/*.pdf'],
  },
})
```

---

## 三、验收标准

### ✅ Footer 验收

- [ ] **桌面端**：联系方式模块向左移动约 4 个文字宽度 (64px)
- [ ] **桌面端**：友情链接模块向左移动约 2 个文字宽度 (32px)
- [ ] **移动端**：布局正常，无溢出
- [ ] **移动端**：单列垂直布局

**视觉效果：**

```
┌────────────────────────────────────────────────────────────┐
│  [Logo] 智能光谱分析实验室                                  │
│  Laboratory for Intelligent Spectral Analysis              │
│                                                            │
│  致力于拉曼与红外光谱技术的智能化创新及其在多领域的精准    │
│  应用... (描述文字占用 2 列宽度)                            │
│                                                            │
│                           ┌───────────────┐  ┌──────────┐  │
│                           │ 联系方式      │  │ 友情链接 │  │
│                           │ (左移 4 文字)  │  │ (左移 2 文 │  │
│                           │ 地址：...     │  │ 中国计量 │  │
│                           │ 邮箱：...     │  │ 量新学院 │  │
│                           │ 电话：...     │  │ 材料与化 │  │
│                           └───────────────┘  └──────────┘  │
└────────────────────────────────────────────────────────────┘
```

---

### ✅ 团队成员页验收

- [ ] **教育经历年份**：字体明显增大 (`text-base` ≈ 16px)
- [ ] **工作经历年份**：字体明显增大 (`text-base` ≈ 16px)
- [ ] **年份格式**：保持左对齐 + 冒号
- [ ] **年份颜色**：`text-gray-700`

**视觉效果：**

```
教育经历

2020 年 6 月—至今：    教授，同济大学医学院...
(text-base，字体增大)

2017 年 2 月—2020 年 5 月：研究员，海军军医大学...
```

---

### ✅ 成果展示页验收

- [ ] **点击 PDF 按钮**：正常打开模态框
- [ ] **加载状态**：显示"PDF 加载中..." + 旋转图标
- [ ] **PDF 显示**：内容正确显示在 iframe 中
- [ ] **错误处理**：加载失败时显示"PDF 加载失败，请尝试在新窗口打开"
- [ ] **在新窗口打开**：按钮可以正常跳转
- [ ] **关闭按钮**：正常关闭模态框

**模态框状态：**

```
┌─────────────────────────────────────────┐
│  PDF 预览                          [×]  │
├─────────────────────────────────────────┤
│                                         │
│           PDF 加载中...                 │
│           (旋转图标)                    │
│                                         │
├─────────────────────────────────────────┤
│              [在新窗口打开] [关闭]      │
└─────────────────────────────────────────┘
```

---

## 四、修改前后对比

### Footer 布局对比

| 项目 | 修改前 | 修改后 |
|------|--------|--------|
| 联系方式位置 | 右移 4rem | 左移 4rem |
| 友情链接位置 | 右移 4rem | 左移 2rem |
| 视觉效果 | 两栏靠右 | 两栏居中偏左 |

---

### 年份字体对比

| 项目 | 修改前 | 修改后 |
|------|--------|--------|
| 字体大小 | `text-sm` (14px) | `text-base` (16px) |
| 可读性 | 较小 | 更大更清晰 |
| 视觉权重 | 较低 | 更高 |

---

### PDF 查看器对比

| 项目 | 修改前 | 修改后 |
|------|--------|--------|
| 加载状态 | 无 | ✅ 显示"PDF 加载中..." |
| 错误处理 | 无 | ✅ 显示错误提示 |
| 按钮功能 | 下载 PDF | ✅ 在新窗口打开 |
| 用户体验 | 较差 | ✅ 友好清晰 |

---

## 五、构建验证

```bash
pnpm run build
```

**构建结果：** ✅ 成功

```
✓ 67 modules transformed.
✓ built in 2.00s

dist/index.html                               1.43 kB │ gzip:  0.82 kB
dist/assets/index-BFDJ57yF.css               81.60 kB │ gzip: 12.54 kB
dist/assets/achievements-CBGll8k-.js         11.58 kB │ gzip:  4.22 kB
dist/assets/team-CH1-3pj0.js                 18.50 kB │ gzip:  4.92 kB
...
```

**警告：** `[unocss] failed to load icon "carbon-account-off"` - 不影响功能

---

## 六、测试建议

### Footer 测试
1. 在桌面端 (1920px) 查看布局
2. 在平板端 (768px) 查看布局
3. 在移动端 (375px) 查看布局
4. 检查各断点过渡是否平滑

### 团队成员页测试
1. 查看 PI 的教育经历年份字体
2. 查看 PI 的工作经历年份字体
3. 对比修改前后的字体大小差异

### 成果展示页测试
1. 点击论文条目的"PDF"按钮
2. 检查模态框是否正常打开
3. 检查 PDF 是否正常加载
4. 测试"在新窗口打开"功能
5. 测试关闭功能

---

## 七、已知问题

### PDF 路径包含中文
- **问题**：某些浏览器可能无法正确显示包含中文字符的 PDF 路径
- **解决方案**：使用"在新窗口打开"按钮
- **长期方案**：将 PDF 文件重命名为英文或数字

---

**文档版本：** v1.0  
**更新时间：** 2026 年 3 月 29 日  
**修改状态：** ✅ 全部完成  
**构建状态：** ✅ 验证通过
