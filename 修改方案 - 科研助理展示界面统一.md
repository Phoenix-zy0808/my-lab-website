# 科研助理展示界面统一修改方案

## 修改目标
将科研助理（research_assistant）的展示界面从**左右分栏布局**改为与本科生、硕士生相同的**垂直列表布局**，统一展示风格。

---

## 当前状态分析

### 现有布局（需要删除）
- **位置**：`src/pages/team.vue` 第 589-799 行
- **代码段**：`<template v-else>` 分支
- **布局类型**：左右分栏布局（左侧成员列表 + 右侧成员详情）
- **特点**：
  - 左侧：小头像 + 姓名 + 职称的按钮列表
  - 右侧：大头像 + 详细信息（联系方式、个人简介、研究兴趣、教育经历、工作经历、代表性论文）
  - 需要点击左侧成员才能查看右侧详情

### 目标布局（参考本科生/硕士生）
- **布局类型**：垂直列表布局
- **特点**：
  - 每个成员一个卡片，垂直排列
  - 左侧：竖长方形头像（h-44 w-32）
  - 右侧：姓名（带下划线）+ 个人简介 + 邮箱 + 荣誉列表
  - 所有信息直接展示，无需点击

---

## 具体修改方案

### 修改文件
**文件**：`src/pages/team.vue`

### 修改位置
**第 589-799 行**：整个 `<template v-else>` 代码块

### 修改步骤

#### 步骤 1：删除现有左右分栏布局代码
删除从第 589 行开始的 `<!-- 其他角色（副教授、科研助理）- 保留左右分栏布局 -->` 注释到第 799 行 `</template>` 结束的整个代码块。

**删除范围**：
```vue
<!-- 其他角色（副教授、科研助理）- 保留左右分栏布局 -->
<template v-else>
  <!-- 白色卡片容器 -->
  <div class="p-8 rounded-xl bg-white shadow-lg">
    ...（中间所有左右分栏布局代码）...
  </div>
</template>
```

#### 步骤 2：替换为垂直列表布局
使用与本科生/硕士生相同的垂直列表布局代码结构，但**不包含分页功能**（科研助理人数通常较少）。

**新代码结构**：
```vue
<!-- 科研助理 - 垂直列表布局 -->
<template v-else-if="selectedRole === 'research_assistant'">
  <!-- 白色卡片容器 -->
  <div class="p-8 rounded-xl bg-white shadow-lg">
    <div class="space-y-8">
      <div
        v-for="member in currentMembers"
        :key="member.id"
        class="pb-8 border-b border-gray-200 flex flex-col gap-6 last:pb-0 last:border-0 sm:flex-row"
      >
        <!-- 左侧头像（竖长方形） -->
        <div class="flex-shrink-0 sm:w-40">
          <div class="mx-auto rounded-lg bg-white h-44 w-32 shadow-md overflow-hidden sm:mx-0 sm:h-52 sm:w-40">
            <img
              v-if="member.photo"
              :src="member.photo"
              :alt="`${member.name} - ${member.title}`"
              class="h-full w-full object-cover"
              loading="lazy"
              decoding="async"
            >
            <div
              v-else
              class="text-3xl text-white font-bold flex h-full w-full items-center justify-center from-primary to-secondary bg-gradient-to-br sm:text-4xl"
            >
              {{ member.name.charAt(0) }}
            </div>
          </div>
        </div>

        <!-- 右侧信息 -->
        <div class="flex-1">
          <!-- 姓名 + 下划线 -->
          <div class="mb-4 text-center sm:text-left">
            <h3 class="text-xl text-gray-800 font-bold pb-2 border-b-2 border-primary inline-block">
              {{ member.name }}
            </h3>
          </div>

          <!-- 简介 + 邮箱 + 荣誉 -->
          <div class="text-gray-700 leading-relaxed">
            <p v-if="member.bio" class="mb-3">
              {{ member.bio }}
            </p>
            <p v-if="member.email" class="text-sm">
              邮箱：
              <a :href="`mailto:${member.email}`" class="text-secondary hover:underline">
                {{ member.email }}
              </a>
            </p>
            <ul v-if="member.honors?.length" class="mt-4 list-disc list-inside space-y-1.5">
              <li v-for="(honor, index) in member.honors" :key="index" class="text-sm text-gray-600 leading-normal">
                {{ honor }}
              </li>
            </ul>
          </div>
        </div>
      </div>
    </div>
  </div>
</template>
```

---

## 样式对比

| 元素 | 本科生/硕士生 | 科研助理（修改后） |
|------|-------------|------------------|
| **容器** | `p-8 rounded-xl bg-white shadow-lg` | 相同 |
| **布局** | `space-y-8` 垂直列表 | 相同 |
| **成员卡片** | `pb-8 border-b border-gray-200 flex flex-col gap-6 sm:flex-row` | 相同 |
| **头像尺寸** | `h-44 w-32`（竖长方形） | 相同 |
| **姓名样式** | `text-xl text-gray-800 font-bold pb-2 border-b-2 border-primary` | 相同 |
| **简介样式** | `text-gray-700 leading-relaxed` | 相同 |
| **邮箱样式** | `text-sm` + 链接 | 相同 |
| **荣誉样式** | `list-disc list-inside` 小圆点列表 | 相同 |
| **分页功能** | 有（人数较多时） | **无**（人数较少，不需要分页） |

---

## 代码逻辑说明

### 条件判断修改
**当前逻辑**：
```vue
<template v-if="selectedRole === 'pi'">
  <!-- PI 介绍 -->
</template>

<template v-else-if="selectedRole === 'undergraduate' || selectedRole === 'graduated_undergraduate'">
  <!-- 本科生 -->
</template>

<template v-else-if="selectedRole === 'master_student' || selectedRole === 'graduated_master'">
  <!-- 硕士生 -->
</template>

<template v-else>
  <!-- 其他角色（科研助理等）- 左右分栏布局 -->
</template>
```

**修改后逻辑**：
```vue
<template v-if="selectedRole === 'pi'">
  <!-- PI 介绍 -->
</template>

<template v-else-if="selectedRole === 'undergraduate' || selectedRole === 'graduated_undergraduate'">
  <!-- 本科生 -->
</template>

<template v-else-if="selectedRole === 'master_student' || selectedRole === 'graduated_master'">
  <!-- 硕士生 -->
</template>

<template v-else-if="selectedRole === 'research_assistant'">
  <!-- 科研助理 - 垂直列表布局 -->
</template>
```

### 数据源
- 使用 `currentMembers` 计算属性（已存在）
- 数据来自 `teamData.value[selectedRole.value]`
- 不需要分页，直接遍历 `currentMembers`

---

## 修改后的效果

### 修改前
```
┌─────────────────────────────────────────┐
│  [小头像] 科研助理1                      │
│  [小头像] 科研助理2         ┌──────────┐ │
│  [小头像] 科研助理3         │  详情    │ │
│                            │  大头像  │ │
│                            │  姓名    │ │
│                            │  简介    │ │
│                            │  教育    │ │
│                            │  论文    │ │
│                            └──────────┘ │
└─────────────────────────────────────────┘
  左侧列表      右侧详情
```

### 修改后
```
┌─────────────────────────────────────────┐
│  ┌────┐                                 │
│  │头像│  科研助理1                       │
│  │    │  简介内容...                     │
│  └────┘  邮箱：xxx@xxx.com              │
│          • 荣誉1                         │
│          • 荣誉2                         │
│  ─────────────────────────────          │
│  ┌────┐                                 │
│  │头像│  科研助理2                       │
│  │    │  简介内容...                     │
│  └────┘  邮箱：xxx@xxx.com              │
│          • 荣誉1                         │
│  ─────────────────────────────          │
│  ...                                    │
└─────────────────────────────────────────┘
  垂直列表，直接展示所有信息
```

---

## 注意事项

### 1. **不需要分页**
- 科研助理人数通常较少（1-5人）
- 不使用 `currentPageData` 或 `currentMasterPageData`
- 直接遍历 `currentMembers` 即可

### 2. **保留 selectedMember 逻辑**
- 虽然科研助理改用垂直列表，但 `selectedMember` 变量仍需保留
- 因为其他角色（PI、硕士生等）仍在使用该变量

### 3. **响应式设计**
- 使用 `flex flex-col gap-6 sm:flex-row` 实现响应式
- 移动端：头像和信息垂直排列
- 桌面端：头像和信息水平排列

### 4. **图片加载优化**
- 保留 `loading="lazy"` 和 `decoding="async"` 属性
- 保留图片加载失败时的降级处理（显示首字母）

---

## 修改文件清单
1. ✅ `src/pages/team.vue` - 1 处修改（第 589-799 行）

**总计：1 处修改，替换整个 `<template v-else>` 代码块**

---

## 测试建议

1. **数据测试**：
   - 测试有科研助理数据的情况
   - 测试无科研助理数据的情况（空状态）
   - 测试科研助理有 honors 数据的情况

2. **页面测试**：
   - 点击"科研助理"标签，验证垂直列表布局
   - 验证头像显示（有图片 vs 无图片）
   - 验证荣誉列表显示（小圆点样式）
   - 验证响应式布局（移动端 vs 桌面端）

3. **交互测试**：
   - 确认不需要点击成员即可查看详情
   - 确认所有信息直接展示在列表中

---

**创建时间**: 2026年4月4日  
**版本**: v1.0
